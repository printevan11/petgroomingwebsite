# TODO
- [x] Fix Navbar logo asset path (import logo from src/assets and update img src)
- [x] Clean up onError fallback markup if it disrupts layout
- [x] Run `npm run build` inside `pet-groom` to confirm no compile errors


